import 'package:flutter/material.dart';

class ContainerErrorAuth extends StatelessWidget {
  const ContainerErrorAuth({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}